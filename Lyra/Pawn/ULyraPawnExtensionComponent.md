### 상속
`UPawnComponent` , `IGameFrameworkInitStateInterface`

### 주요 책임
- AbilitySystemComponent 초기화 및 종료
- 이 Pawn의 기능을 정의하는 데이터를 가지고 있음

### 주요 프로퍼티
`ULyraPawnData` : [[ULyraPawnData]] 를 여기 둠, Replicated